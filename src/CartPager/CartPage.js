import React, { Component } from 'react'
import { Text, View, FlatList, TouchableOpacity, NativeModules, PermissionsAndroid, Modal, Button } from 'react-native'
import { Icon, SearchBar, Divider } from 'react-native-elements'

export default class CartPage extends Component {

    render() {
        return (
            <View style={{ flex: 1 }}>
                <Text>CartPage</Text>
            </View>
        )
    }
}
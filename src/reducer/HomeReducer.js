import { START_LOADING,LOADING_SUCCESS,LOADING_FAILED } from "./../actions/Actions";
const initialState = {
    selectedApps:[]
}

const HomeReducer = (state=initialState,action)=>{
    return state
}

export default HomeReducer;